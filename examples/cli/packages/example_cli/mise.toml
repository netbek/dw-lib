[tools]
python = "3.13"

[settings]
lockfile = true
minimum_release_age = "7d"
python.uv_venv_auto = "create|source"

[hooks.enter]
run = """
mise install --quiet
uv sync --all-extras --all-groups --quiet
"""
