[project]
name = "dw-lib"
version = "1.4.0"
description = "Tools for working with Postgres, ClickHouse, and DuckDB."
readme = "README.md"
requires-python = "==3.13.*"
dependencies = [
    "boto3>=1.41.1",
    "clickhouse-connect>=0.10.0",
    "clickhouse-sqlalchemy",
    "duckdb>=1.4.2",
    "duckdb-engine>=0.17.0",
    "httpx>=0.28.1",
    "jinja2>=3.1.6",
    "psutil>=7.1.3",
    "psycopg2-binary>=2.9.11",
    "pydantic>=2.12.4",
    "pydantic-settings>=2.12.0",
    "pydash>=8.0.5",
    "pyyaml>=6.0.3",
    "sqlalchemy>=2.0.44",
    "sqlglot>=28.0.0",
    "sqlmodel>=0.0.27",
    "sqlparse>=0.5.3",
]

[project.optional-dependencies]
dbt = [
    "dbt-clickhouse",
    "dbt-core>=1.10.0,<1.11", # Pinned because of dbt-clickhouse
    "livereload>=2.7.1",
    "polyfactory>=3.0.0",
    "pydantic>=2.12.4",
    "pydash>=8.0.5",
    "pyyaml>=6.0.3",
]
loader = [
    "boto3>=1.41.1",
    "chdb>=3.7.2",
    "jinja2>=3.1.6",
    "psycopg2-binary>=2.9.11",
    "pydantic>=2.12.4",
    "rich>=14.2.0",
]
peerdb = [
    "clickhouse-connect>=0.10.0",
    "clickhouse-sqlalchemy",
    "httpx>=0.28.1",
    "psycopg2-binary>=2.9.11",
    "pydantic>=2.12.4",
    "pydash>=8.0.5",
    "pyyaml>=6.0.3",
    "rich>=14.2.0",
    "sqlalchemy>=2.0.44",
    "sqlglot>=28.0.0",
    "sqlmodel>=0.0.27",
]

[dependency-groups]
dev = [
    "pytest>=8.4.2",
    "pytest-docker>=3.2.3",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.isort]
force_alphabetical_sort = true
line_length = 100
profile = "black"
py_version = 313

[tool.ruff]
line-length = 100
target-version = "py313"

[tool.ruff.lint]
ignore = ["D100", "D101", "D102", "D103", "D104", "D107", "D205"]

[tool.pytest.ini_options]
addopts = "--no-header --tb=no --container-scope=module"
filterwarnings = [
    "ignore::DeprecationWarning:dbt_common.invocation", # https://github.com/dbt-labs/dbt-core/issues/9791
    "ignore::DeprecationWarning:dbt.cli.options"        # https://github.com/dbt-labs/dbt-core/issues/12038
]

[tool.uv.sources]
clickhouse-sqlalchemy = { git = "https://github.com/netbek/clickhouse-sqlalchemy.git", rev = "datetime-uuid" }
dbt-clickhouse = { git = "https://github.com/netbek/dbt-clickhouse.git", rev = "v1.9.7-batch" }
